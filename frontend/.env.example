# Frontend environment. Copy to `frontend/.env` (git-ignored).

# Base URL of the backend API.
#   empty  -> fully standalone, in-browser MockConversationService (GitHub Pages)
#   set    -> talk to the backend (e.g. http://localhost:8787 in dev)
VITE_API_BASE_URL=
